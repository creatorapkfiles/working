package SplitLinkedListinParts;
import ListNode.ListNode;

public class Solution {
    public ListNode[] splitListToParts(ListNode head, int k) {
        if (head == null || k <= 0) {
            return null;
        }

        ListNode[] parts = new ListNode[k];
        ListNode currentPart = head;
        for (int i = 0; i < k; i++) {

        }
        return null;
    }
}
